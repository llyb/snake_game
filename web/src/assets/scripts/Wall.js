import GameObject from './GameObject';

export default class Walll extends GameObject {
    constructor(ctx, L) {
        super();
        this.ctx = ctx;
        this.L = L; // 小正方形的边长
        ctx.fillStyle = ;
        ctx.fillRect();
    }
}
